{{WORKSPACE_DIR}}/logs/whatsapp-bridge.log
{{WORKSPACE_DIR}}/logs/whatsapp-adapter.log {
    daily
    rotate 7
    compress
    missingok
    notifempty
    create 0640 {{USER}} {{GROUP}}
    postrotate
        systemctl kill -s HUP lobster-whatsapp-bridge 2>/dev/null || true
    endscript
}
